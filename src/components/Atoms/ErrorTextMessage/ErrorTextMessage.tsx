import styled from 'styled-components/macro';
import { theme } from '../../../theme/mainTheme';

export const ErrorTextMessage = styled.p`
  text-align: center;
  color: ${theme.error};
  font-weight: bold;
`;
