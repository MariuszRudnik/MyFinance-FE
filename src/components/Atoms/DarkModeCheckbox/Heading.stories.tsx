import React from 'react';
import { storiesOf } from '@storybook/react';
import { DarkModeCheckbox } from './Checkbox';

storiesOf('Atoms/DarkModeCheckbox', module).add('Normal', () => <DarkModeCheckbox />);
